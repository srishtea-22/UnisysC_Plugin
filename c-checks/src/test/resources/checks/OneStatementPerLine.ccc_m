void sayHello() {
  float t=2.4;int a=9; // Noncompliant 

  printf("Hello World!"); printf("Hello World!"); // Noncompliant
  
  if (a) {} // OK

  if (a) {} if (b) {} // Noncompliant

  while (condition); // OK

  while (condition) { // OK
    break; // OK
  }
}
